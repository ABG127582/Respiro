// AI Features have been disabled.
// This file is kept as a stub to prevent import errors if it's referenced elsewhere, 
// though all references should have been removed.

export const generatePatternFromIntent = async (userIntent: string): Promise<any | null> => {
  return null;
};

export const getAIInsight = async (hrDiff: number, hrvDiff: number, vagalScore: number): Promise<string> => {
   return "Sessão registrada.";
}